<?php

require('database.php');

/**
 * find availabilities around given coord
 */
function getPoints($user, $num, $long, $lat) {
	
}

/**
 * find availabilities within bounding box
 */
function getPointsInBox($user, $minLong, $maxLong, $minLat, $maxLat) {
	
}
